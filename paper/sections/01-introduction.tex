\section{Introduction}

Agent runtimes increasingly retain an append-only record of model calls, tool
results, human decisions, state updates, and coordination events.  Event
sourcing makes several desirable properties seem immediate.  If state is a
fold over the log, a run can be replayed.  If a child log shares a prefix with a
parent, a run can be forked without recomputing that prefix.  If causes are
recorded, artifacts can be traced back to model and tool activity.  ActiveGraph
states these properties as consequences of making the log the source of truth
\cite{nakajima2026log}.

This paper formalizes and empirically checks the replay and fork properties
claimed on architectural grounds in Nakajima (2026).  The relationship is
deliberate: the earlier work describes a deployed architecture, while the
present work asks which side conditions make its architectural claims sound.
Negative results about the originating runtime are therefore part of the
contribution rather than exceptions to be hidden.

Two gaps motivate the work.  First, ``deterministic replay'' often means
deterministic replay \emph{after oracle outputs have been fixed}.  This is
useful, but the phrase suppresses operational questions.  Was the model output
captured faithfully?  Is a request retained without its result?  Does replay
serve the recorded result or make a new call?  Is the runtime itself
schedule-independent, or does a particular FIFO order merely produce one
repeatable execution?

Second, event sourcing in a business application normally reconstructs
application state; it does not claim to reverse the external world.  That
distinction becomes load-bearing for agents.  A retrospectively discarded log
suffix may contain an email, a charge, a delete, a publication, or a billable
model call.  The forked trace can omit the effect, but the world cannot be
assumed to be one in which the effect never happened.  A correct fork contract
must say which property it preserves.

Figure~\ref{fig:replay-family} names seven assertions commonly compressed into
the word ``replay.''  They are ordered here from artifact identity toward
claims about execution and the external world, but they do not form a simple
implication chain.  A system can preserve a decision while changing its path,
or reproduce bytes while retaining too little in-log evidence to execute.

\begin{figure}[ht]
\centering
\small
\begin{tabularx}{0.96\textwidth}{@{}>{\bfseries}p{0.22\textwidth}Y@{}}
\toprule
Assertion & Question answered \\
\midrule
Byte & Is the serialized artifact identical under its verifier? \\
Trace & Are the same ordered semantic events retained? \\
Projection & Does folding those events yield equivalent selected state? \\
Path & Is the model, tool, and action sequence the same? \\
Decision & Is the task-level conclusion equivalent? \\
Execution & Can code re-execute with zero live calls by serving recorded results? \\
Environment & Does the observed external world match the world claimed by the prefix? \\
\bottomrule
\end{tabularx}
\caption{Replay is a family of assertions.  Each row requires its own
observation and evidence contract.}
\label{fig:replay-family}
\end{figure}

Table~\ref{tab:assertion-map} makes the scope of those seven assertions
explicit.  The four fork properties operationalize only the assertions needed
to decide whether a particular cut may be projected, executed, continued, or
treated as a counterfactual world.  Path and Decision remain application-level
equivalences rather than independent fork properties in this artifact.

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\textwidth}{@{}P{0.14\textwidth}P{0.34\textwidth}Y@{}}
\toprule
Assertion & Operationalization in this paper & Evidence boundary \\
\midrule
Byte & Source-artifact verifier; not a fork property & External capsule verification can establish byte identity without raising a run's log-derived grade. \\
Trace & ExactTrace and the checked prefix identities & The observed-fork audit compares semantic prefix fields modulo declared lineage exclusions. \\
Projection & ProjectionReplay under ProjectedState or FactsOnly$(K)$ & A fixed, well-formed ordered prefix is sufficient; no effect execution occurs. \\
Path & ExactTrace restricted to model, tool, and action events when a source schema exposes them & Not independently assessed in the submitted cut matrix; equal decisions need not imply equal paths. \\
Decision & A source-specific task-level equivalence when decisions are present & Not an independent fork property or replay-grade license in this artifact. \\
Execution & StrictExecutionReplay & Boundary is the first run-level grade that licenses recorded-effect re-execution in general; the cut-level assessment remains authoritative. \\
Environment & ExternalContinuation and CounterfactualWorld$(\Omega)$ & No replay grade suffices alone; footprint coverage, a target-environment premise, and any reconciliation obligations are also required. \\
\bottomrule
\end{tabularx}
\caption{Cross-map from the replay assertion family to the paper's formal and
empirical scope.  Replay grades summarize retained evidence; they are not a
one-to-one replacement for these assertions.}
\label{tab:assertion-map}
\end{table}

The familiar reducer shape

\begin{equation}
  A : S \times E \rightarrow S \times F^*
\end{equation}

is not the contribution.  It is a Mealy-style transition system
\cite{mealy1955method}, resembles the Elm update architecture
\cite{elmarchitecture}, and appears in functional event sourcing through the
Decider pattern \cite{chassaing2021decider}.  The operational seam needed here
is a scheduled step over configurations plus an interpreter outcome tied back
to a request identity:

\begin{equation}
  \mathsf{step}:C\times\Sigma\rightarrow C,
  \qquad
  \mathsf{interpret}:R\rightarrow
  \{\mathsf{Committed},\mathsf{Failed},\mathsf{Unknown}\}.
\end{equation}

The second function is outside the pure kernel.  Evidence links its result to
the request in the log.  This makes scheduling, incomplete boundaries, and
already-realized external effects explicit without claiming a new minimal
agent calculus.

Table~\ref{tab:prior-claims} states the relationship to the predecessor paper
directly.  The earlier architecture remains useful; the present paper supplies
the contract under which its claims are licensed.

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\textwidth}{@{}P{0.18\textwidth}P{0.20\textwidth}Y@{}}
\toprule
Claim in \cite{nakajima2026log} & Status here & Qualification \\
\midrule
Deterministic replay & Upheld, qualified & A fixed ordered log projects deterministically.  Schedule-independent log production and zero-call execution require separate conditions (Sections~\ref{sec:model}--\ref{sec:fork}). \\
Cheap forking & Upheld, qualified & Prefix copying is structurally cheap; continuation and external-world claims are property-relative (Sections~\ref{sec:fork} and~\ref{sec:validation}). \\
End-to-end lineage & Upheld in the observed scope & The 121 observed children preserve compared prefix fields.  Stronger replay licenses require request/outcome and provenance evidence (Sections~\ref{sec:grades} and~\ref{sec:validation}). \\
\bottomrule
\end{tabularx}
\caption{Claim status relative to \emph{The Log is the Agent}.}
\label{tab:prior-claims}
\end{table}

\paragraph{Contributions.}
This manuscript makes four contributions.

\begin{enumerate}[leftmargin=*]
  \item It defines property-relative fork assessment for projection, strict
  execution replay, external continuation, and a declared external-world
  observation.

  \item It separates effect evidence along external footprint, replay source,
  and lifecycle, allowing a recorded result and an irreversible footprint to
  coexist in one descriptor.

  \item It gives an executable scheduler and preserves counterexamples showing
  that settlement need not terminate or converge.  Disjoint declared writes
  remain insufficient when reads and emitted triggers interfere.

  \item It checks the contracts against 5,540 public runtime logs, every cut in
  those logs, and 121 observed forks.  A public offline conformance fixture
  additionally crosses a committed recorded oracle boundary: its generator
  observes no second fixture-oracle call, and its verifier checks a fork-bound
  receipt and caller assertion.
\end{enumerate}

\paragraph{Genre.}
This is not a mechanized-proof paper.  Model identities are stated as
propositions; empirical claims are encoded as properties, checked over
generated families with FsCheck, and evaluated exhaustively over the cuts of
the available logs.  We call these executable contracts or checked properties
in the running text.  A failed property produces a minimized, named
regression fixture.  Passing checks are finite evidence over the specified
generators and artifacts, not universal proofs.  This choice is intentional:
calculi with
machine-checked composition, termination, and information-flow results already
exist \cite{liu2026lambdaa,garby2026llmbda}.  Our comparative advantage is an
executable runtime contract connected to real runtime artifacts.

\paragraph{Roadmap.}
Section~\ref{sec:model} defines the executable model and observations.
Section~\ref{sec:confluence} reports termination and confluence results.
Section~\ref{sec:fork} defines fork properties and effect conditions.
Section~\ref{sec:grades} connects those conditions to replayability grades.
Sections~\ref{sec:method} and~\ref{sec:validation} describe the artifact and
empirical validation.  We then position the work, discuss implications, and
state limitations.
